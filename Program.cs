using System;

namespace LinQ_Problems
{
    class Program
    {
        static void Main(string[] args)
        {
            var words = new WordsList().GetWords();
            var th = WordsList.Where(word = th);
            foreach (var word in WordsList)
            {
                Console.WriteLine("the", "this", "tenth");
            }
             
        }    
    }
}
